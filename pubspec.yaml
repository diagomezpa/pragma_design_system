name: pragma_design_system
description: A Flutter package implementing a Design System based on Atomic Design principles for e-commerce applications.
version: 1.0.0
homepage: https://github.com/your-username/pragma_design_system

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: